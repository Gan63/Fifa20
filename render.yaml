services:
  - type: web
    name: fifa20-analytics
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn backend.app:app
    branch: main
    healthCheckPath: /
    envVars: {}
